#include "main.h"
/**
 * handle_logic - handles logical operators
 * @args: tokenized user input
 * 
 * Return: modified args
*/
char *handle_logic(char *command, char **envp)
{
	/*Nmeshindiwa hapa*/
	int i = 0;
	char *token = strtok()
	while (args[i])
	{
		return (NULL);
		i++;
	}
}
